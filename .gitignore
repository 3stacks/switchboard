# secrets — NEVER commit (treat this repo as public)
.env
secrets/
*.secret.conf

# rendered Asterisk config (contains creds / PII — only the .template is committed)
asterisk/pjsip.conf
asterisk/globals.conf

# python
agent/.venv/
agent/.venv312/
__pycache__/
*.pyc

# agent runtime state (session id + last result — machine-local)
agent/state/

# audio / call dumps
*.wav
*.raw
*.slin
recordings/

# misc
.DS_Store
*.log
agent/voices/
