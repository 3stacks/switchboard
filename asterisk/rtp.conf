;==============================================================
; switchboard — RTP media port range.
; Open these UDP ports if the host firewall is on. A registration trunk behind
; NAT with rtp_symmetric + rewrite_contact (set on the endpoints) needs no STUN.
;==============================================================
[general]
rtpstart=10000
rtpend=10100
